---
description: Revisit your "why" — goal, history, profile. Forces Discovery mode.
argument-hint: [what changed / what to revisit — optional]
---

This command **forces the Discovery route**. Carry it out **in this one turn**, all in the same context — do not split it across turns:

1. **Read `src/pace/SKILL.md`** only to (a) apply the zero-state guard and (b) build the forwarded context bundle `{config, profile, zones, active_week}` from state.
2. **Then immediately `Read src/pace-discovery/SKILL.md` and `src/pace-discovery/references/vision-write.md`** into this same context and **continue as the Discovery coach**, running the flow through to the coach's message.

This is a **silent context operation, not a handoff**: emit no "routing / handing off" text, and **do not end the turn** on the route decision. The Discovery coach resolves `[surface].language` from the bundle's `config` and speaks it from its first token.

Athlete message: $ARGUMENTS

If no vision exists yet, this is a full Discovery (and, on a brand-new athlete, runs the intake that seeds `athlete/profile.json` with markers / level / equipment). If a vision exists, this is a targeted partial re-Discovery — the vision is amended, never rewritten.
