# <Method name> — method pack (TEMPLATE)

> Copy this file when you create a method pack. Replace every section below. The Planner reads this prose to understand the strategy and draws its sessions from `session_structures.csv`. Keep it short and concrete.

**Principle.** State the core idea in one or two sentences — what this strategy optimises and why.

**Intensity distribution.** Describe how the week's intensity is distributed (e.g. ~80/20 polarized, pyramidal, block). Be explicit per phase if it changes across base/build.

**Who it suits.** The athlete profile, weekly volume, and goals this method fits.

**Contraindications / when not to use.** When this strategy is a poor fit (low volume, injury, phases that exclude intensity, etc.).

**Conformance.** This pack restricts *how* the plan distributes intensity; it never overrides `periodization-rules.csv`. Every session in `session_structures.csv` must be phase-legal. Keep at least one active-recovery session in the catalog.
